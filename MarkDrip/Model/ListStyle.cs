namespace MarkDrip.Model;

/// <summary>
/// 定义列表的样式。
/// </summary>
public enum ListStyle
{
    /// <summary>
    /// 使用项目符号（-、*、+）的无序列表。
    /// </summary>
    Bullet,

    /// <summary>
    /// 使用数字标记（1.、2. 等）的有序列表。
    /// </summary>
    Ordered,
}
